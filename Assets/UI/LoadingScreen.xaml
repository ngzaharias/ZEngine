<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:noesis="clr-namespace:NoesisGUIExtensions" 
  mc:Ignorable="d" 
  Style="{DynamicResource Style.Page}" 
  d:DesignWidth="1280" 
  d:DesignHeight="720">
  <Page.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Themes/Default.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Page.Resources>
  <Grid Background="{StaticResource Brush.Background0}">
    <Viewbox>
      <Grid Width="1280" Height="720">
        <Grid x:Name="Loading" d:IsHidden="True">
          <Grid.Style>
            <Style TargetType="Grid">
              <Style.Triggers>
                <DataTrigger Binding="{Binding IsSplash}" Value="True">
                  <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Grid.Style>
          <TextBlock Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource Style.SubHeader.Text}" Margin="0" FontSize="60"/>
        </Grid>
        <Grid x:Name="Splash">
          <Grid.Style>
            <Style TargetType="Grid">
              <Style.Triggers>
                <DataTrigger Binding="{Binding IsSplash}" Value="False">
                  <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Grid.Style>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
          </Grid.RowDefinitions>
          <Image Source="Textures/T_Logo.png" Grid.Row="1" Grid.Column="1"/>
          <TextBlock TextWrapping="Wrap" Grid.Row="2" Grid.Column="1" Foreground="{DynamicResource Brush.Foreground}" FontSize="50" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0" Text="by Papa Koala" Grid.ColumnSpan="2"/>
        </Grid>
      </Grid>
    </Viewbox>
  </Grid>
</Page>