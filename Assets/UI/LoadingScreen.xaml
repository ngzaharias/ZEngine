<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d" Style="{DynamicResource Style.Page}" xmlns:noesis="clr-namespace:NoesisGUIExtensions" d:DesignWidth="1280" d:DesignHeight="720">
  <Page.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Themes/Default.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Page.Resources>
  <Grid Background="{StaticResource Brush.Background0}">
    <Viewbox>
      <Grid Width="1280" Height="720">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="4*"/>
          <ColumnDefinition Width="5*"/>
          <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="2*"/>
          <RowDefinition Height="10*"/>
          <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource Style.SubHeader.Text}" Grid.RowSpan="3" Margin="0" Grid.ColumnSpan="3" FontSize="60"/>
        <ProgressBar Background="{StaticResource Brush.Background0}" Foreground="{StaticResource Brush.Accent}" BorderBrush="{DynamicResource Brush.Foreground}" BorderThickness="8,8,8,8" Value="{Binding Progress}" Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom" Height="50" Visibility="Collapsed"/>
      </Grid>
    </Viewbox>
  </Grid>
</Page>