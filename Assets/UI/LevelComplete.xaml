<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignWidth="1920"
  d:DesignHeight="1080" Style="{DynamicResource Style.Page}">
  <Page.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Themes/Default.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Page.Resources>
  <Grid Style="{StaticResource Style.Fade}">
    <Viewbox Margin="0">
      <Grid Width="1280" Height="720">
        <Grid.RowDefinitions>
          <RowDefinition Height="2*"/>
          <RowDefinition Height="4*"/>
          <RowDefinition Height="4*"/>
          <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="4*"/>
          <ColumnDefinition Width="3*"/>
          <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Grid.ColumnSpan="3" Margin="0" Grid.Row="1">
          <TextBlock Text="Level Complete!" TextWrapping="Wrap" FontSize="81"/>
        </Grid>
        <StackPanel VerticalAlignment="Center" Grid.Column="1" Margin="0" Grid.Row="2">
          <Button Content="Exit to Menu" Style="{StaticResource Style.Button}" Command="{Binding ExitToMenuCommand}"/>
          <Button Content="Exit Game" Style="{StaticResource Style.Button}" Command="{Binding ExitGameCommand}"/>
        </StackPanel>
      </Grid>
    </Viewbox>
  </Grid>
</Page>