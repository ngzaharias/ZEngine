<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:noesis="clr-namespace:NoesisGUIExtensions" 
  mc:Ignorable="d" 
  Style="{DynamicResource Style.Page}" 
  d:DesignWidth="1280" 
  d:DesignHeight="720" Focusable="True">
  <Page.InputBindings>
    <KeyBinding Key="Escape" Command="{Binding CloseCommand}"/>
    <KeyBinding Key="Space" Command="{Binding CloseCommand}"/>
    <MouseBinding MouseAction="LeftClick" Command="{Binding CloseCommand}"/>
  </Page.InputBindings>
  <Page.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Themes/Default.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <Storyboard x:Key="Loaded">
        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Text" Storyboard.TargetProperty="(UIElement.Opacity)">
          <LinearDoubleKeyFrame KeyTime="0:0:1" Value="0"/>
          <LinearDoubleKeyFrame KeyTime="0:0:2" Value="1"/>
          <LinearDoubleKeyFrame KeyTime="00:00:03" Value="1"/>
          <LinearDoubleKeyFrame KeyTime="0:0:3.800" Value="0"/>
        </DoubleAnimationUsingKeyFrames>
        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Logo_Game" Storyboard.TargetProperty="(UIElement.Opacity)">
          <LinearDoubleKeyFrame KeyTime="0:0:4" Value="0"/>
          <LinearDoubleKeyFrame KeyTime="0:0:5" Value="1"/>
        </DoubleAnimationUsingKeyFrames>
        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Background" Storyboard.TargetProperty="(UIElement.Opacity)">
          <LinearDoubleKeyFrame KeyTime="0:0:6" Value="0"/>
          <LinearDoubleKeyFrame KeyTime="0:0:8" Value="1"/>
        </DoubleAnimationUsingKeyFrames>
      </Storyboard>
    </ResourceDictionary>
  </Page.Resources>
  <Page.Triggers>
    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
      <BeginStoryboard Storyboard="{StaticResource Loaded}" />
    </EventTrigger>
  </Page.Triggers>
  <Grid Background="{StaticResource Brush.Background0}">
    <Viewbox>
      <Grid Width="1280" Height="720">
        <Grid.Style>
          <Style TargetType="Grid">
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsSplash}" Value="False">
                <Setter Property="Visibility" Value="Collapsed"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </Grid.Style>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="1*"/>
          <ColumnDefinition Width="2*"/>
          <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="1*"/>
          <RowDefinition Height="2*"/>
          <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Image x:Name="Background" Grid.ColumnSpan="3" Grid.RowSpan="3" Source="Textures/Pages/Page03/T_Background.png" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0"/>
        <Image x:Name="Logo_Game" Source="Textures/T_Logo.png" Grid.Row="1" Grid.Column="1" Opacity="0"/>
        <TextBlock x:Name="Text" TextWrapping="Wrap" Grid.Row="1" Grid.Column="1" Foreground="{DynamicResource Brush.Foreground}" FontSize="120" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0" Text=" Papa&#x0a;Koala" Opacity="0" RenderTransformOrigin="0.5,0.5">
          <TextBlock.RenderTransform>
            <TransformGroup>
              <RotateTransform Angle="-3"/>
            </TransformGroup>
          </TextBlock.RenderTransform>
        </TextBlock>
      </Grid>
    </Viewbox>
  </Grid>
</Page>