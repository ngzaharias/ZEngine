<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:noesis="clr-namespace:NoesisGUIExtensions" 
  mc:Ignorable="d" 
  Style="{DynamicResource Style.Page}" 
  d:DesignWidth="1280" 
  d:DesignHeight="720">
  <Page.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Themes/Default.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Page.Resources>
  <Grid Background="{StaticResource Brush.Background0}">
    <Viewbox>
      <Grid Width="1280" Height="720">
        <Grid.RowDefinitions>
          <RowDefinition Height="5*"/>
          <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Bottom" Style="{StaticResource Style.SubHeader.Text}" Margin="0" FontSize="20"/>
      </Grid>
    </Viewbox>
  </Grid>
</Page>